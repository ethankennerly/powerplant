package com.finegamedesign.powerplant
{
    /* Query objects in display list by their class. */
    public class Contract extends Card
    {
        /* Hide.  Add to stage.  Swap image.  Then show. */
        public function Contract(cardValue:int = Card.NULL) {
            super(cardValue);
        }
    }
}
